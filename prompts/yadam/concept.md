# Profile: yadam
# Stage: yadam.concept.options.v1
# Schema: schemas/yadam/concept-options.schema.json

Please generate concept options for a Korean historical story (Yadam).

## Instructions
1. You must output a JSON object strictly matching the schema `schemas/yadam/concept-options.schema.json`.
2. Do not include any filesystem, shell, network, provider, or implementation instructions.
3. Input IDs are immutable and must not be altered.
4. Echo the input `conceptInputsHash` byte-for-byte in the output. Do not invent or rewrite reference or history provenance.
5. All text content must be in Korean.
6. Return JSON only (no backticks, no extra text, strictly JSON).
